services:
  db:
    image: postgres:17
    container_name: wfap-postgres
    # restart: unless-stopped
    restart: no
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
#     volumes:
#       - wfap_pgdata:/var/lib/postgresql/data

# volumes:
#   wfap_pgdata: