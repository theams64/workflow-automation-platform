# Workflow Automation Platform

A workflow automation platform where users can connect external services (via API keys or Oauth, then create workflows made of triggers + steps, and the system executes them on a schedule or event, recording runs, logs, and retries. 
