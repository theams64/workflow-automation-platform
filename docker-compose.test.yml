services:
  db:
    image: postgres:17
    container_name: test-wap-postgres
    # restart: unless-stopped
    restart: no
    environment:
      POSTGRES_DB: ${TEST_POSTGRES_DB}
      POSTGRES_USER: ${TEST_POSTGRES_USER}
      POSTGRES_PASSWORD: ${TEST_POSTGRES_PASSWORD}
    ports:
      - "5433:5432"
#     volumes:
#       - test_wap_pgdata:/var/lib/postgresql/data

# volumes:
#   test_wap_pgdata: